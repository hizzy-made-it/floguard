# Public frontend → FastAPI backend origin (no trailing slash, no /api suffix).
# Used by src/lib/api.js as `${REACT_APP_BACKEND_URL}/api`.
REACT_APP_BACKEND_URL=https://floguard-backend.onrender.com
