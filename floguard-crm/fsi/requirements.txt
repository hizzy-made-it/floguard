# FloGuard FSI — Python batch/cron/API deps (FULL stack)
# Versions verified against PyPI 2026-07-20. `>=` so a fresh install still resolves.

# --- Geo compute ---
whitebox>=2.3.6
richdem>=0.3.4
GDAL>=3.13.1            # prefer conda-forge if the pip wheel fails: conda install -c conda-forge gdal
rasterio>=1.5.0
rioxarray>=0.22.0
geopandas>=1.1.4
shapely>=2.1.2
polars>=1.42.1
# --- Data / ORM ---
geoalchemy2>=0.20.0
psycopg[binary]>=3.2.0
sqlalchemy>=2.0.0
# --- API ---
fastapi[standard]>=0.139.2
# --- Data source client ---
httpx>=0.27.0           # NWS/NOAA, Open-Meteo, FEMA NFHL REST
